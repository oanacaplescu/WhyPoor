import SwiftUI

enum AppTheme {
    static let sand = Color(hex: "#E5CB90")
    static let cream = Color(hex: "#FFF3C8")
    static let teal = Color(hex: "#34A99D")
    static let slate = Color(hex: "#458393")

    static let accent = teal
}
